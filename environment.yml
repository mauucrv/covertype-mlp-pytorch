name: covertype-mlp
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - jupyter
  - ipykernel
  - pip:
      - torch==2.9.0
      - torchvision==0.24.0
      - torchaudio==2.9.0
      - scikit-learn>=1.5
      - pandas>=2.2
      - numpy>=1.26
      - matplotlib>=3.9
      - seaborn>=0.13
      - optuna>=4.0
      - tqdm
